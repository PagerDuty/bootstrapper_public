Description:
    The bootstrapper_model generator creates an empty bootstrap file in db/bootstrapper.

    The generator takes one argument which will be the name of the bootstrap file.

Examples:
    ./script/generate bootstrapper_model User

        This will create:
            Bootstrap:     db/bootstrapper/users.rb